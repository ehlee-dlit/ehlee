[백업]333
dropbox
Google Drive
One Drive

[비교도구]
diff tool

[버전무시]
.gitignore 파일을 만들어 적어둔다  => 버전관리하지않을경우

branch

commit id 이용

tag : commit id 에 이름을 붙임



===> sourceTree

bitbucket.org / heeya61


===> 비교도구
kdiff3

termux==========
pkg install nano
y
nano


posix ===============
pwd
cd /
cd ~


git init
git status
git add hello1.txt
git log
git diff
git reset --hard : 삭제
git log -p
la -al
git log
git checkout 로그아이디  (확인하기)
cat hello.txt
git checkout master
git add filename
git commit -m "3"
git commit -am "4" (add 와 commit 한번에) - untracked는 빼고
git config --global core.editor "nano" - 나노에디터로 편집기를 쓰겠다
git reset --hard 로그아이디 (로그아이디 버전으로 되겠다)  - 해당버전까지만 살림

git hosting ====
push

gifHub
gitLab.com



remote add 

[github.com 이용하기]

echo "# ehlee">> README.md 
git init 
git add README.md 
git commit -m "first commit" 
git remote add origin https://github.com/ehlee-dlit/ehlee.git
 git push -u origin master


git remote add origin https://github.com/ehlee-dlit/ehlee.git
 git push -u origin 마스터




